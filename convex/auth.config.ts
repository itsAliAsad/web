const authConfig = {
    providers: [
        {
            domain: "https://caring-cattle-16.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
